# 应用生成预览域名（对话页 iframe，路径为 {codeGenType}_{appId}）
VITE_APP_PREVIEW_HOST=/api/static

# 应用部署作品域名（查看作品，路径为 {deployKey}）
VITE_APP_DEPLOY_HOST=/api/static

VITE_DEPLOY_DOMAIN=http://localhost

VITE_API_BASE_URL=/api
